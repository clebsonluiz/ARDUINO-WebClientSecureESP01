#define WIFI_SSID "SSID_NAME"

#define WIFI_PASS "PASSWORD"

#define HOST_ADDRESS "<https-host>.com"

#define HOST_ROUTE "/api/"

#define API_KEY_NAME "X-API-KEY"

#define API_KEY_VALUE "<api.key-value>"

/**
 * To access sites with ssl certificates, needs the fingerprint resume certificate to allow access
 **/
#define FINGERPRINT "00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00";
